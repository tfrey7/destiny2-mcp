/tmp
/out-tsc

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.vscode/*

# Local Claude Code config (enabled plugins, etc.)
.claude/

/dist

# MCPB build artifacts (staging tree + packed .mcpb)
/release

# Secrets — never commit
.env
oauth.txt

.DS_Store

# Ornament enrichment cache (downloaded screenshots + intermediate captions)
.cache/

# High-res icon source kept locally for re-cropping; only the packed icon.png is tracked
assets/*@source.png

# Throwaway local preview artifacts
previews/
